function [S,D,ro1,eps] = jtsk2kar(Y,X)
    %% Constants
    ro = pi/180;
    S0 = (78 + 30/60)*ro;
    R0 = 1298039.0046;
    n=sin(S0);
    %%

    R = sqrt(x^2+y^2)
    D_ = atan2(Y,X)

    S = 2*(atan2(tan(S0/2+pi/4)*(R0/)))
end